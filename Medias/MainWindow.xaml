<Window x:Class="Medias.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Medias"
        mc:Ignorable="d"
        Title="MainWindow" 
        WindowStartupLocation="CenterScreen"
        Height="650" Width="1200"
        MinHeight="650" MinWidth="1200"
        MaxHeight="650" MaxWidth="1200">
    <Grid >
        <DataGrid x:Name="dataGrid" AutoGenerateColumns="False" Margin="0,44,0,0" FontSize="16">
            <DataGrid.Columns>

                <DataGridTextColumn Header="Name" Width="150" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Description" Width="300" Binding="{Binding Description}"/>
                <DataGridTemplateColumn Header="Genres" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding GenresString}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Authors" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding AuthorsString}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Time" Width="100" Binding="{Binding Duration}"/>
                <DataGridTextColumn Header="Status" Width="150" Binding="{Binding Status}"/>

            </DataGrid.Columns>
        </DataGrid>
        <TextBox HorizontalAlignment="Left" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Width="720" Height="39" FontSize="16"/>
        <Button Content="Add" HorizontalAlignment="Left" Margin="997,0,0,0" VerticalAlignment="Top" Height="39" Width="78" FontSize="16" Click="Add_Click"/>
        <Button Content="Save" HorizontalAlignment="Left" Margin="1104,0,0,0" VerticalAlignment="Top" Height="39" Width="78" FontSize="16" Click="Save_Click"/>
        <ComboBox HorizontalAlignment="Left" Margin="725,0,0,0" VerticalAlignment="Top" Width="139" Height="39"  FontSize="16"/>
    </Grid>

</Window>
